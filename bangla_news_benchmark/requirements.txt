# Core dependencies
torch>=2.0.0
transformers>=4.30.0
datasets>=2.12.0
accelerate>=0.20.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.65.0

# Optional: For TPU support (Google Cloud TPU VMs only)
# Uncomment the following lines when running on TPU:
# torch-xla>=2.0.0
# cloud-tpu-client

# Installation instructions for TPU:
# pip install torch~=2.0.0 torch_xla[tpu]~=2.0.0 -f https://storage.googleapis.com/libtpu-releases/index.html
